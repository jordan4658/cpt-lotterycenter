<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caipiao.live.common.mybatis.mapperext.lottery.LotteryFavoriteMapperExt">
    <resultMap id="BaseResultMap" type="com.caipiao.live.common.mybatis.entity.LotteryFavorite">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="lottery_id" property="lotteryId" jdbcType="INTEGER"/>
        <result column="update_at" property="updateAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="update" parameterType="java.util.List">
        insert into lottery_favorite (user_id, lottery_id)
        values
        <foreach collection="lotteryFavorites" item="lotteryFavorite" separator="," index="index">
            (#{lotteryFavorite.userId,jdbcType=INTEGER}, #{lotteryFavorite.lotteryId,jdbcType=INTEGER})
        </foreach>
    </insert>

    <delete id="delete" parameterType="java.util.List">
        delete from lottery_favorite
        where user_id=#{userId}
        and lottery_id in
        <foreach collection="lotteryFavoriteIds" item="lotteryFavoriteId" separator="," open="(" close=")">
            #{lotteryFavoriteId,jdbcType=INTEGER}
        </foreach>
    </delete>

</mapper>